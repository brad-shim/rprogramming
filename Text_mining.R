# 
install.packages("dplyr")
library(dplyr)

# Quantitative Analysis of Textual Data
install.packages("quanteda")
library("quanteda")

#
install.packages("tidytext")
library("tidytext")

#
install.packages("tibble")
library("tibble")

#
install.packages("tm")
library("tm")

tribble(~id, ~name, ~price,
        "A001", "Mouse", 30000)

tribble(~id, ~name, ~price,
        "A001", "Mouse", 30000,
        "A002", "Keyboard", 50000,
        "A003", "Monitor", 150000)



